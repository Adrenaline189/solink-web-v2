{
  "//": "Base configuration. You can override any numeric/string values by environment variables. See comments below.",
  "baseUrl": "https://api-solink.network",
  "farmType": "extension_farm",
  "amountPerShot": 50,
  "bursts": 40,
  "concurrency": 5,
  "dailyCap": 2000,
  "wallets": [
    {
      "wallet": "demo_wallet_test1"
      // "token": "OPTIONAL_REAL_JWT"  // Leave empty to let the script perform demo-login.
    },
    {
      "wallet": "demo_wallet_test2"
      // IMPORTANT: Do NOT put a placeholder like STATIC_JWT_TOKEN_IF_YOU_HAVE.
      // If token is omitted, the script will call /api/auth/demo-login for this wallet.
    }
  ]

  // Notes:
  // - ENV overrides:
  //   BASE_URL, TYPE, AMOUNT, BURSTS, CONCURRENCY, CAP
  //   WALLETS="demo_wallet_test2" or WALLETS="demo_wallet_test1,demo_wallet_test2"
  //
  // - If demo_wallet_test2 keeps getting 401:
  //     (1) Make sure you did NOT put a fake token in config.
  //     (2) Ensure backend allows demo-login for that wallet.
}
