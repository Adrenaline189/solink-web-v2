registry=https://registry.npmjs.org/
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
audit=false
fund=false
progress=false
# ถ้ามี peer deps งอแงระหว่าง build ให้ปลดค่านี้ (คงไว้ถ้าจำเป็น)
legacy-peer-deps=true
